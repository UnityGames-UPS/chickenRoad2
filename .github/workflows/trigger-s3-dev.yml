name: Trigger S3 Upload

on:
  push:
    branches:
      - B2B-DevBuild
  workflow_dispatch:    

jobs:
  call-reusable:
    uses: UnityGames-UPS/unity-build-pipeline/.github/workflows/main.yml@main
    with:
      s3_bucket: unity.build-dev-b2b
      s3_folder: OT-CR2
      cloudfront_distribution_id: E378YMF6D9XCK8
      invalidate_paths: "/OT-CR2/*"